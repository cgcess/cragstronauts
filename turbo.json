{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["CLOUDFLARE_API_TOKEN"],
  "globalPassThroughEnv": ["ZERO_API_KEY", "ZERO_API_URL"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["VITE_CLERK_PUBLISHABLE_KEY", "VITE_VAPID_PUBLIC_KEY"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "env": ["VITE_CLERK_PUBLISHABLE_KEY", "VITE_VAPID_PUBLIC_KEY"],
      "persistent": true,
      "cache": false
    },
    "@cragstronauts/api#dev": {
      "dependsOn": ["@cragstronauts/web#build"],
      "persistent": true,
      "cache": false
    },
    "deploy": {
      "dependsOn": ["build"],
      "cache": false
    },
    "@cragstronauts/api#deploy": {
      "dependsOn": ["@cragstronauts/web#build"],
      "cache": false
    },
    "typecheck": {}
  }
}
